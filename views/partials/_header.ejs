<nav class="navbar navbar-expand-md navbar-dark bg-dark" style="padding: 10px 30px;"> <!-- Increased padding for more space and a bigger header -->
  <a class="navbar-brand" href="/" style="margin-right: 20px;"> <!-- Adds space between the logo and the page border -->
    <img src="/images/logo.jpeg" alt="Logo" height="50" class="d-inline-block align-top logo-image"> 
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <% if (session && session.userId) { %>
        <a class="nav-link" href="/auth/logout">Logout</a>
        <% } else { %>
        <a class="nav-link" href="/auth/login">Login</a>
        <% } %>
      </li>
      <li class="nav-item">
        <% if (session && session.userId) { %>
        <a class="nav-link" href="/history">History</a>
        <% } %>
      </li>
    </ul>
  </div>
</nav>

<style>
  .logo-image {
    filter: brightness(120%); 
    border: 2px solid gold; 
    border-radius: 50%; 
    padding: 5px; 
  }

  .navbar {
    min-height: 80px; 
  }
</style>
